#include <iostream>
using namespace std;

// function of print the all charater of an array with using function
void printArray(char arr[], int size)
{
    cout << "print the all array is : ";
    for (int i = 0; i <size; i++)
    {
        cout << arr[i] << " ";
    }
    cout << endl;

}
int main()
{
    /*
        int example[5]; // array declare
         // accessing an array
         cout<< " value of 4 index" << example[4] << endl;
    */
    /*
    int first[12] = {1,3,4}; // initailsing the array
    cout<<"value of 11 index"<< first[11] << endl; // acccessing an elemnt
    */

    // print array,s elemnt 0 index to n
    /* int array[82] = {1, 3, 42, 43, 86}; // initalising the array
      int n;
      cout << " please enter the value of index [n]";
      cin >> n;
      // print the array
      for (int i = 0; i < n; i++)
          cout << array[i] << "  ";
    */

    // program to find size of an array
    //    int mukesh[14]= {4};
    //      int mukeshsize = sizeof(mukesh)/sizeof(int);
    //      cout<< " size of mukesh array is : " <<mukeshsize << endl;





// print the all charater of an array with using function
  /*  char ch[7]= {'d','e','w','a','r','h','g'};
    
   printArray(ch, 7);
    cout << "Everythig is ok" << endl;
    return 0; */

}